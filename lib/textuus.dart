import 'package:flutter/material.dart';

class ReusedText extends StatelessWidget {
  const ReusedText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('hello comrade, hope your good',style: TextStyle(color: Colors.blueGrey),);
  }
}