import 'package:flutter/material.dart';

class SpecialFood extends StatelessWidget {
  const SpecialFood({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Special Food"),
    );
  }
}